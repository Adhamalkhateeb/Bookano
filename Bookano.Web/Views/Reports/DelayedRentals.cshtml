@model DelayedRentalsViewModel
@{
    ViewData["Title"] = "Delayed Rentals Report";
}


<div class="card mb-4">
    <div class="card-header">
        <div class="card-title">
            <span>Delayed Rentals</span>
        </div>
        @if (Model.Rentals.Any())
        {
            <div class="card-toolbar gap-3">
                <a class="btn btn-light-success w-100 w-md-auto" asp-action="ExportDelayedRentalsToExcel">
                    <i class="fa-solid fa-file-excel"></i>
                    Export to Excel
                </a>
                <a class="btn btn-light-danger w-100 w-md-auto" asp-action="ExportDelayedRentalsToPDF">
                    <i class="fa-solid fa-file-pdf"></i>
                    Export to PDF
                </a>
            </div>
        }
    </div>
    <div class="card-body">
        @if (Model.Rentals.Any())
        {
            <div class="table-responsive">
                <table class="table table-row-dashed table-row-gray-300 gy-2 align-middle js-responsive-table">
                    <thead>
                        <tr class="fw-bold fs-5 text-white bg-black">
                            <th class="ps-4 rounded-start">Subscriber ID</th>
                            <th>Subscriber Name</th>
                            <th>Subscriber Mobile</th>
                            <th>Book Title</th>
                            <th>Book Serial</th>
                            <th>Rental Date</th>
                            <th>End Date</th>
                            <th>Extended On</th>
                            <th class="pe-4 rounded-end">Delay in Days</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var rental in Model.Rentals)
                        {
                            <tr>
                                <td data-label="Subscriber ID" class="ps-md-4" >@rental.SubscriberId</td>
                                <td data-label="Subscriber Name">@rental.SubscriberName</td>
                                <td data-label="Subscriber Mobile">@rental.SubscriberMobile</td>
                                <td data-label="Book Title">@rental.BookTitle</td>
                                <td data-label="Book Serial">@rental.BookSerialNumber</td>
                                <td data-label="Rental Date">@rental.RentalDate.ToString("d MMM, yyyy")</td>
                                <td data-label="End Date">@rental.EndDate.ToString("d MMM, yyyy")</td>
                                <td data-label="Extended On"> @(rental.ExtendedOn is null ? "-" : rental.ExtendedOn.Value.ToString("d MMM, yyyy"))</td>
                                <td data-label="Delay in Days"class="pe-4">@rental.DelayInDays</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {

            <h5>No delayed rentals</h5>
        }

    </div>
</div>


@section Scripts {
    <script src="~/js/reports.js" asp-append-version="true"></script>
}